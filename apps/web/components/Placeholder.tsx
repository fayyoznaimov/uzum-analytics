import AppShell from './AppShell'; import { ArrowRight, Construction, Sparkles } from 'lucide-react';
export default function Placeholder({title,subtitle,features}:{title:string;subtitle:string;features:string[]}){return <AppShell title={title} subtitle={subtitle}><section className="panel placeholder"><div className="placeholder-icon"><Construction/></div><h2>Каркас раздела готов</h2><p>Этот модуль подключим к реальным данным на следующем этапе. Навигация и дизайн уже заложены.</p><div className="feature-chips">{features.map(x=><span key={x}><Sparkles size={15}/>{x}</span>)}</div><button className="primary">Следующий этап <ArrowRight size={16}/></button></section></AppShell>}
